#include<iostream>

using namespace std;

typedef long long LL;

LL n, a, b;

int main()
{
	cin >> n >> a >> b;
	for(int i = 1; i <= n; i++)
		cin >> w[i];
	LL left = 1, right = 5e5;
	while(left < right)
	{
		LL mid = (left + right) / 2;
		if(check(mid)) 
	}
	return 0;
}