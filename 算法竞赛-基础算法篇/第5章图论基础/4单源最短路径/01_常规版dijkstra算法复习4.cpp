#include<iostream>
#include<vector>
#include<utility>
#include<algorithm>
#include<cmath>

using namespace std;

typedef pair<int, int> PII;

const int N = 1e4 + 10;
const int INF = 0;
vector<PII> edges[N];
int dist[N];
bool st[N];

int main()
{
	cout << (int) pow(2, 31) - 1 << endl;
	return 0;
}